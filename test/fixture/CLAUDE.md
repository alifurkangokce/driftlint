# Fixture project

The entry point is `src/app.ts` and helpers live in `lib/util.ts`.
Old docs referenced `src/removed-module.ts` which was deleted long ago.

Build with `npm run build`, deploy with `npm run deploy:prod`.

```bash
npm run build
make release
```

<!-- driftlint-ignore -->
This hypothetical `docs/never-existed.md` line must NOT be flagged.
