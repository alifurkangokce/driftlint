---
name: wordy
description: This description is deliberately long enough to overflow the tiny 200-character budget configured in .driftlintrc.json for this fixture, proving the config file's skillBudget value is respected when no CLI flag overrides it at scan time.
---

Body.
