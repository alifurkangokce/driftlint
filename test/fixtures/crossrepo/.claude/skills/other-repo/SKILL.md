---
name: other-repo-helper
description: Describes files that live in a completely different repository.
---

Edit `src/one.ts`, then `src/two.ts`, then `lib/three.ts`.
Also touch `app/four.ts`, `src/five/six.ts`, and `config/seven.yml`.
